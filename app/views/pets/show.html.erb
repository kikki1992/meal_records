<div class="row">
  <aside class="col-sm-4">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= @pet.name %></h3>
      </div>
    </div>
  </aside>
  <div>
    <%= link_to 'レコードの追加', new_record_path(pet_id: @pet), class: 'btn btn-lg btn-primary' %>
  </div>
  <%= render 'pets/records', records: @records %>
  
  
  
  
  
  
</div>